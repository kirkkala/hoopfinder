services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: hoopfinder
      POSTGRES_PASSWORD: hoopfinder
      POSTGRES_DB: hoopfinder
    ports:
      - "5432:5432"
    volumes:
      - hoopfinder-pg:/var/lib/postgresql/data

volumes:
  hoopfinder-pg:
